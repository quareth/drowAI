{
  "tool_id": "shell.exec",
  "test_cases": {
    "minimal": {
      "description": "Required fields only - simple command",
      "params": {
        "command": "whoami"
      },
      "expected_valid": true
    },
    "full": {
      "description": "All optional fields populated",
      "params": {
        "command": "ls -la",
        "cwd": "artifacts",
        "env": {"MY_VAR": "value"},
        "yield_time_ms": 1000,
        "max_output_chars": 4096,
        "max_runtime_sec": 60
      },
      "expected_valid": true
    },
    "edge_cases": [
      {
        "description": "Network reconnaissance command",
        "params": {
          "command": "ping -c 4 127.0.0.1"
        },
        "expected_valid": true
      },
      {
        "description": "File listing with options",
        "params": {
          "command": "find . -name '*.txt' -type f"
        },
        "expected_valid": true
      },
      {
        "description": "Bounded yield for delayed output",
        "params": {
          "command": "cat /proc/version",
          "yield_time_ms": 500
        },
        "expected_valid": true
      },
      {
        "description": "Command with environment variables",
        "params": {
          "command": "echo $HOME",
          "env": {"HOME": "/tmp/test"}
        },
        "expected_valid": true
      },
      {
        "description": "Long runtime bound for slow operations",
        "params": {
          "command": "sleep 1 && echo done",
          "max_runtime_sec": 300
        },
        "expected_valid": true
      }
    ],
    "invalid": [
      {
        "description": "Legacy transport field rejected by Phase 3 contract",
        "params": {
          "command": "whoami",
          "transport": "pty"
        },
        "expected_valid": false,
        "expected_error": "extra_forbidden"
      },
      {
        "description": "Legacy timeout field rejected by Phase 3 contract",
        "params": {
          "command": "sleep 1",
          "timeout_sec": 300
        },
        "expected_valid": false,
        "expected_error": "extra_forbidden"
      },
      {
        "description": "Legacy idempotency field rejected by Phase 3 contract",
        "params": {
          "command": "whoami",
          "idempotent": true
        },
        "expected_valid": false,
        "expected_error": "extra_forbidden"
      },
      {
        "description": "Legacy redaction field rejected by Phase 3 contract",
        "params": {
          "command": "whoami",
          "redact_output": false
        },
        "expected_valid": false,
        "expected_error": "extra_forbidden"
      },
      {
        "description": "Destructive rm -rf / blocked by denylist",
        "params": {
          "command": "rm -rf /"
        },
        "expected_valid": false,
        "expected_error": "denylist"
      },
      {
        "description": "Fork bomb blocked by denylist",
        "params": {
          "command": ":(){:|:&};:"
        },
        "expected_valid": false,
        "expected_error": "denylist"
      },
      {
        "description": "Privilege escalation blocked",
        "params": {
          "command": "sudo su"
        },
        "expected_valid": false,
        "expected_error": "denylist"
      },
      {
        "description": "Network listener blocked",
        "params": {
          "command": "nc -l -p 8080"
        },
        "expected_valid": false,
        "expected_error": "denylist"
      },
      {
        "description": "System reboot blocked",
        "params": {
          "command": "reboot"
        },
        "expected_valid": false,
        "expected_error": "denylist"
      }
    ]
  },
  "expected_command_patterns": {
    "minimal": ["sh", "-lc", "whoami"],
    "full": ["sh", "-lc", "ls -la"]
  }
}
