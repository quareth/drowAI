"""Terminal collaborators for the cloud runner provider.

This package owns provider-side cloud runner terminal transport collaborators.
It does not import the provider facade or artifact/tool-command operations.
"""
