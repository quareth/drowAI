"""Cloud runner dispatch collaborators.

This package owns remote runner-control dispatch helpers. It does not own
operation result polling, terminal orchestration, tool-command handling, or the
public runtime-provider facade.
"""
