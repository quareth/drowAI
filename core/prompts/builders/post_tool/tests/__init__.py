"""Tests for the post-tool prompt builder package.

This package hosts focused tests for read-only projection helpers used by
``think_more`` (and, in a future migration, by PTR). The tests treat each
helper as a black box and exercise its public API only.
"""
