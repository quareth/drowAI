## Instructions

Produce the response now by filling the four structured fields from the system prompt:

1. `action`
2. `findings`
3. `impact`
4. `recommended_next_action`

Do not include headings inside the field values. Stay grounded in the supplied sections. Do not restate facts the prior assistant turn already conveyed; advance the engagement. If a canonical prior-turn context block is present, treat it as authoritative and do not claim exact quotes unless that block supports them. If a retry-history block is present, reflect it in `action` but do not narrate the full chain. If an active-decision or effective-goal block is present, use it as the anchor for `recommended_next_action`.
